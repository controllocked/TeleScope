"""telescope package marker.

This file exists to satisfy the required repo layout. The runtime entry point
is app.py, which can be invoked directly. We avoid adding extra files to keep
structure minimal and explicit for an MVP.
"""
